export enum TransactionType {
    FUND = 'FUND',
    TRANSFER = 'TRANSFER',
    WITHDRAW = 'WITHDRAW',
}

export enum TransactionStatus {
    PENDING = 'PENDING',
    SUCCESS = 'SUCCESS',
    FAILED = 'FAILED'
}